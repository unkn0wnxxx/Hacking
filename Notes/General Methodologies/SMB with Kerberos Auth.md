
When password & NTLM Auth is disabled and we can only access stuff with Kerberos Authentication, we can utilize the following tool:

```
impacket-smbclient scrm.local/ksimpson@dc1.scrm.local -k -no-pass
```

```
shares
use Public
get Network Security Changes.pdf 
```
